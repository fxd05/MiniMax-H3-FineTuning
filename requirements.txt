# Python 3.12 (NGC PyTorch 26.03 container) + CUDA 13.2 environment; installed by install_env.sh.
# Inference-related packages are pinned to the verified working environment
# (t2va/ref2va generation on this box). The diffusers/transformers pins are
# exact because the H3 classes (MiniMaxH3Transformer3DModel,
# AutoencoderKLMiniMaxH3, diffusers.modular_pipelines.minimax_h3.packing) are
# not in a released wheel.
torch==2.11.0a0+a6c236b9fd.nv26.3.46836102
# ^ NGC 26.03 container build (torch-2.11.0a0+a6c236b9fd.nv26.3.46836102-cp312-...whl,
# not on PyPI); on a non-NGC host keep plain torch>=2.8 instead.
diffusers @ git+https://github.com/huggingface/diffusers.git@abc5e9bf71fd38f53cd471bc3acaa84bc5ecbfdc
# diffusers@abc5e9bf imports huggingface_hub.get_cached_repo_tree, which only
# exists in hub >= 1.0 (the 0.37-0.99 range is absent from the cluster mirror),
# and its metadata requires hub>=1.23. The diffusers install is --no-deps, so
# this must be installed explicitly.
huggingface-hub==1.26.0
# transformers 4.57.x hard-requires huggingface-hub<1.0 at import time — an
# empty intersection with the hub pin above. 5.0.0 allows hub<2.0 and keeps
# Qwen3-VL, which the H3 text encoder needs.
transformers==5.0.0
accelerate==1.14.0
deepspeed>=0.18
# peft 0.17 imports transformers.HybridCache, removed in transformers 5.0;
# 0.20.0 is the first version verified against the 5.0.0 + hub 1.26 combo.
peft==0.20.0
av==18.0.0
safetensors==0.7.0
# 0.7.0 is below diffusers@abc5e9bf's declared >=0.8.0, but that install is
# --no-deps so the env's 0.7.0 is what runs; transformers needs >=0.4.3.
numpy==2.1.0
Pillow==12.1.1
